# QuarkfanTools 3.0 Completion Audit

Last updated: 2026-08-16

This audit treats the 2.x product as a capability baseline and the 3.0 center contracts as the only acceptable implementation boundary. A green test or visible button is not sufficient evidence unless the owning center, persistence, policy, audit and end-to-end path are covered.

## Requirement evidence

| Requirement | State | Authoritative evidence |
| --- | --- | --- |
| MG channel/account isolation, normalized messages, history, cursor, route, delivery and Lark identity | Implemented | `Message-Gateway/src`, 12 unit/integration tests, Compose E2E message delivery and history backfill |
| CH source, retrieval, transcript and short/mid/long memory governance | Implemented | `Context-Hub/src`, 7 tests, Compose E2E ingestion/retrieval |
| MH multi-provider, multi-model-type routing, round-robin/random and failover | Implemented | `Model-Hub/src`, 4 tests, Compose E2E provider routing and failover |
| CR Skill/MCP/App/Suite/Workflow/Command lifecycle, conflict handling and built-ins | Implemented | `Capability-Registry/src`, 21 tests, Compose E2E command/workflow/custom App execution |
| Runtime isolation, sessions, workspaces, continuation and multiple runtime adapters | Implemented | `Runtime-Center/src`, 26 tests, Compose E2E execution and continuation |
| Scheduler cron/daily/weekly/interval, next/last run, immediate run, logs and backfill | Implemented | `Scheduler-Center/src`, 13 tests, Compose E2E automatic/manual scheduling and history backfill |
| Resource content store, ACL, cleanup, diagnostics and FFmpeg operations | Implemented | `Resource-Center/src`, 12 tests, Compose E2E resource integrity, media and diagnostic ZIP |
| Governance policy, approval/resume, credential encryption and audit | Implemented | `Governance-Center/src`, 6 tests, Browser approval/resume E2E |
| Authenticated Dashboard with account/password, RBAC and forced bootstrap password change | Implemented | `Platform-Console`, 15 tests, desktop/mobile Playwright coverage over 14 pages |
| Browser deterministic and model-planned tasks with isolated sessions and evidence | Implemented | `Runtime-Center/src/browser.ts`, private-network tests, Compose E2E approval/resume and artifacts |
| Linux deployment and operational health | Implemented | `Platform-Deployment/compose.yaml`; eleven application services healthy on `zwj-ubuntu` |
| Production dependency vulnerability audit | Verified | `npm audit --omit=dev --audit-level=high` returned zero vulnerabilities for all ten Node packages on 2026-08-16 |
| Independent GitHub repositories and parent submodule publication | Blocked externally | Four original center repositories are pushed; seven new repository names return GitHub 404 and browser authentication is not complete |

## Controlled degradation

- WeCom historical desktop polling is not emulated on Linux. MG reports the adapter as planned/degraded and preserves a remote-worker migration path.
- DingTalk is explicitly unsupported until a real adapter exists.
- Existing desktop Chrome profiles are not mounted into the server. Browser authentication uses encrypted managed sessions and an external-auth checkpoint.
- Claude execution requires an externally configured Claude runtime; other runtime adapters remain available.

These states are acceptable only while they remain visible in API/UI capability status and never report false success.

## Hardening status

- Every independently deployable process rejects a missing or weak internal service token at startup.
- Browser session encryption requires a base64 source key that decodes to exactly 32 bytes.
- Compose services drop all Linux capabilities unless a service explicitly proves it needs one.
- Backups cover PostgreSQL and every persistent application volume, include SHA-256 and archive checks, exclude live secrets, and state the external secret-escrow requirement.
- Restore is guarded, re-verifies its input, creates a pre-restore backup and ends with a full smoke check.
- Console status distinguishes process liveness from dependency readiness and reports version, latency and last-check time.
- Diagnostic ZIP collection uses bounded metadata, recursively redacts structured values and redacts common secret forms inside log text.
- Acceptance and Dashboard QA scripts use exit traps to remove temporary accounts/services and restore production Browser Worker policy after success, failure or interruption.

The remaining publication action is to create and populate the seven new GitHub repositories before pushing the parent submodule commit.
