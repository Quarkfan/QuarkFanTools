# QuarkfanTools 3.0 Current Release

Last updated: 2026-08-16

## Release position

This is the first deployable QuarkfanTools 3.0 server platform. The 2.x product is used as a capability and behavior baseline only. No Electron IPC, customer filesystem path, plaintext credential, legacy page structure, or direct cross-layer call is part of the 3.0 contract.

Every migrated ability is exposed through its owning center and uses tenant/Bot identity, durable state, credential references, policy decisions, approvals, audit records and observable execution state as applicable.

## Running topology

| Service             | Implemented responsibility                                                                                                                                                                                                                                                                                                      |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Message Gateway     | Lark/webhook channels, replaceable SDK/CLI backend contracts, long connection/webhook intake, normalization, deduplication, message history, cursor, route, sink retry, delivery, reactions, attachments, bot identity, user OAuth and governed Lark Sheets/Base data operations                                                |
| Context Hub         | sources, bindings, records, freshness, scoped retrieval, transcripts, memory candidates, confirmed long-term memory, forgetting and generation traces                                                                                                                                                                           |
| Model Hub           | OpenAI-compatible, Anthropic, Ollama, Stable Diffusion WebUI and custom HTTP providers; text/image/audio/video model declarations; fixed/round-robin/random routing, failover, health, usage and capability export                                                                                                              |
| Capability Registry | Skill ZIP/Git import, conflict preview/new/old/edit resolution, review rules, MCP transports, Node App sandbox, Suite, Workflow, Command, bindings, diagnostics and built-in Browser/Media/Office/Lark data capabilities                                                                                                        |
| Runtime Center      | Bot isolation, revisioned Runtime Profiles, Cordis-composed Runtime Providers, immutable admission snapshots, append-only Session Event Ledger, workspaces, model-tool-loop, OpenAI Agents and externally configured Claude provider, workflow runner, Web chat and one governed capability facade                              |
| Scheduler Center    | interval/daily/weekly/cron schedules, timezone, next/last run, immediate run, retry, misfire, logs, continuation and cursor-based history backfill Beta                                                                                                                                                                         |
| Resource Center     | content-addressed storage, ACL, freshness, integrity repair, cleanup plans, diagnostics ZIP, FFmpeg/ffprobe jobs, resource limits and artifacts                                                                                                                                                                                 |
| Governance Center   | policy decisions, owner approval, exact approval resume checks, encrypted credentials, credential rotation, recursive redaction and audit                                                                                                                                                                                       |
| Platform Console    | Better Auth accounts, role-based BFF, forced initial password change, hierarchical and secondary navigation, compact dialog-based page guidance, explicit advanced-setting help, stable single-row list/detail flows, mutation progress/feedback, Runtime Provider/Profile and cross-center plugin management, searchable manual, Bot chat, Browser task/workflow UI, scheduling visibility, logs, diagnostics and system assistant |
| Platform Deployment | PostgreSQL, internal networks, least-privilege workers, persistent volumes, Compose health, source sync, backup, smoke and end-to-end acceptance                                                                                                                                                                                |

Console authentication keeps public HTTPS and operator loopback access as separate cookie surfaces over one account database. Public requests retain Secure cookies. Exact `127.0.0.1`, `localhost` and `::1` hosts may use host-local non-Secure cookies for an SSH tunnel; no external Host receives that policy. The login form verifies the resulting session before reloading and shows an actionable error if credential validation succeeded but the browser rejected the session cookie.

## Built-in capability surface

- Playwright deterministic workflows and Model Hub-planned Browser Agent tasks.
- Encrypted reusable browser sessions, external-auth waiting, approval checkpoints, screenshots, PDF, downloads, trace and video artifacts.
- FFmpeg information, trim, merge, resize, crop, rotate, color, audio, text, subtitles, speed, export, compression, thumbnail, normalization, fade, watermark, GIF and blur operations.
- Word, Excel/CSV, PowerPoint and video Skills over governed Resource/Context contracts.
- Lark Sheets range read/batch update and Lark Base record query/batch update. MG owns channel identity and OAuth; CR exposes the capability; writes require governance approval.
- Business templates are packages, never platform-core branches. The hotel check-in planner performs strict unique ID matching, normalization, conflict checks, idempotent update planning and masked output without customer tokens or table identifiers.

## Controlled degradation

- WeCom historical polling from the 2.x desktop CLI is not silently emulated on Linux. Its channel type reports `planned/degraded`; webhook integration is available, and a future remote desktop worker can implement account-bound history polling without contaminating MG.
- The production Lark backend currently uses the Node SDK/OpenAPI implementation. The external CLI adapter is represented honestly as unavailable until its runtime probe and contract suite pass; MG can inject it without changing Message Hub, storage, routing or Console contracts.
- DingTalk remains an explicit unsupported channel descriptor until an adapter is implemented.
- Browser Worker defaults to isolated headless Chromium. Access to localhost/private networks is blocked, including DNS-resolved private addresses. Deployment administrators may explicitly enable private networks for a trusted isolated installation.
- A user's existing desktop Chrome profile is not mounted into the Linux worker. Authentication uses encrypted managed sessions and external-auth waiting.
- Claude Agent SDK execution is available only when its external runtime endpoint and credential are configured. `model-tool-loop` and OpenAI-compatible execution remain available without it.

These are visible capability states, not successful placeholders. They satisfy the platform rule that unavailable host-specific behavior must fail honestly and offer a migration path.

## Verification evidence

On 2026-08-16:

- All eleven production application services and the HTTPS edge passed Docker health checks on `zwj-ubuntu`.
- Every module passed its unit tests, TypeScript check and production build.
- Compose E2E passed model routing, failover, MG-to-Runtime delivery, session continuation, command/workflow/custom App execution, scheduler manual/automatic runs, history backfill, browser approval/resume and artifacts, media jobs, Context ingestion, resource integrity and diagnostics ZIP.
- Online and quiesced production backups covering PostgreSQL and all five persistent application volumes passed SHA-256, `pg_restore` catalog and archive readability verification. The quiesced path waited for all services to become healthy before returning. The restore command rejected a missing confirmation flag and the verifier rejected tampered metadata.
- The complete Compose acceptance suite passed again after a full stack restart, including routing/failover, persistence, idempotent content reuse, scheduling, browser approval/resume, media and diagnostics.
- Dashboard Playwright validation visited 16 pages at 1440x1000 and 390x844 with no horizontal overflow or clipped button/input/select controls. Configuration acceptance entered create/detail views, including Runtime Profile creation, opened advanced settings and returned to each list. The permanent usage manual was included; no server page screenshots were exported.
- Acceptance-only database rows and attributable persistent artifacts were removed after a verified full backup. Default administrator, system assistant, context records and built-in capability data were retained. Twenty-three legacy encrypted browser-state directories and governance decisions without tenant attribution were intentionally preserved because their ownership cannot be proven.
- The disposable QA account was removed, the mock model was stopped, Browser Worker private-network blocking was restored, and the final production smoke test passed.
- The server keeps an exact parent-and-eleven-module source manifest generated by the guarded release sync. Source and Compose preflight must pass before every image rollout.

The plugin architecture release has 142 passing repository tests across the ten Node projects. Runtime's 35 tests include PluginKernel scope/disposal contracts, Provider lifecycle/Profile admission and Session Event Ledger replay evidence. The seven center-local catalogs have common lifecycle, restart recovery and disabled-provider reactivation contracts; Console tests enforce admin-only mutation at the BFF. Every changed project also passes TypeScript checking and a production build. Server deployment evidence is recorded after the image rollout in this document and `Platform-Deployment/STATUS.md`.

Application source release `7650ea8` was synchronized and rebuilt on `zwj-ubuntu` after verified online backup `20260816T043509Z`. All twelve production services passed smoke. Seven center schemas expose 32 durable Provider records; a noncritical MH adapter remained disabled through restart, retained its event history, and then recovered through `verified` to `active`. Full cross-center acceptance and sixteen-page desktop/mobile UI acceptance passed afterward. The canonical public TLS endpoint remains blocked outside the host by an upstream Tencent Cloud/ICP gate: public HTTP reaches Caddy, loopback SNI returns `200`, but public TLS is reset before packets reach the host. This infrastructure gate does not change the deployed application result and must be cleared through the cloud provider or domain filing rather than by weakening TLS.

The latest Console interaction release was deployed after verified backup `20260816T055728Z`. Production Console code is `c338794`; repository head `5099e0b` adds the final handoff evidence only. Its secondary navigation, page-specific guidance, stable form controls, single-row lists, overflow action menus and mutation feedback passed 19 Console tests plus strict Playwright acceptance over 32 desktop/mobile states. Loopback authentication and all twelve service health checks passed after rollout. Public reachability must be re-probed before acting on the older TLS observation because the user subsequently reported successful domain access.

The Console form/help refinement was deployed from parent `6deaeaa`, Console application `2309841` and Deployment `10b0f62` after verified online backup `20260816T065006Z`. It gives text inputs and selects an explicit shared height, moves contextual page guidance into a dismissible dialog, explains advanced model/provider fields including safe custom-header use, and groups model routing policies by purpose. UI acceptance opened every guidance dialog and verified all three sections across 32 desktop/mobile states before configuration/layout checks; global overflow and clipped controls were zero. Complete cross-center acceptance, loopback authentication and the final twelve-service smoke check passed. Evidence-only repository heads are Console `ec05ccc` and Deployment `b2891e5`.

## Operations entry

Use `Platform-Deployment/README.md`, `Platform-Deployment/docs/operations.md` and `Platform-Deployment/docs/release-handoff.md`. The Console remains bound to server loopback. Production public access is provided by the deployment's Caddy HTTPS profile at `https://tool.quarkfan.com`; port 80 redirects to the canonical HTTPS domain and authentication uses secure cookies.

## Extension boundaries

DeepSeek Cordis Core `4.0.1` remains exact-pinned behind the QuarkfanTools PluginKernel facade and is now in the Runtime production composition path. Cordis never crosses center APIs and is not treated as a sandbox. Untrusted executable capabilities remain in workers/processes/containers.

The full DeepSeek Harness is still a candidate isolated Runtime Provider, not the platform core. Runtime Session Ledger projections and provider-specific streaming/resume contracts can continue to evolve without changing Bot or cross-center contracts.
